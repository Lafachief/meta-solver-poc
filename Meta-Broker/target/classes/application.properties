# Existing properties...

# Logging configuration
logging.level.root=INFO
logging.level.com.metasolver.model.MatchingEngine=INFO
logging.level.com.metasolver=INFO

# Create separate file for self-trade logs
logging.file.name=logs/meta-solver.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# Set specific logger for self-trade prevention
logging.level.com.metasolver.model.MatchingEngine=DEBUG 